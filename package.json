{
  "name": "skillforge",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:desktop": "npx tailwindcss -i input.css -c tailwind-desktop.config.js -o shared/tailwind-desktop.css",
    "build:mobile": "npx tailwindcss -i input.css -c tailwind-mobile.config.js -o shared/tailwind-mobile.css",
    "build": "npm run build:desktop && npm run build:mobile"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.11",
    "tailwindcss": "^3.4.19"
  }
}
